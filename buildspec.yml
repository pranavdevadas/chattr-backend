version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Building TypeScript project...
      - npm run build
artifacts:
  files:
    - '**/*'
  discard-paths: no
